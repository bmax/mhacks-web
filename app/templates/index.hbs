<h2 id='title'>Welcome to Drivers!</h2>
     	<p>
			{{#if session.isAuthenticated}}
			You're logged in as: {{session.full_name}}
			<br/>
			<a {{ action 'invalidateSession' }}>Logout</a>
			{{else}}
				{{#link-to 'login'}}Login{{/link-to}}<br/>
				{{#link-to 'register'}}Register{{/link-to}}
			{{/if}}
		</p>